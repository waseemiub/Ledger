name: Build Transactor V2.7 Urdu Classic APK

on:
  workflow_dispatch:
  push:
    paths:
      - 'Transactor_Android_V2_7_URDU_CLASSIC.zip'
      - '.github/workflows/build-apk.yml'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Gradle 8.9
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'

      - name: Extract Transactor V2.7
        run: |
          rm -rf extracted
          mkdir -p extracted
          unzip -q Transactor_Android_V2_7_URDU_CLASSIC.zip -d extracted

          echo "=== EXTRACTED CONTENT ==="
          find extracted -maxdepth 3 -type f | sort | head -100

      - name: Verify V2.7 project
        run: |
          test -f extracted/Transactor_Android_V2_7_URDU_CLASSIC/settings.gradle.kts
          test -f extracted/Transactor_Android_V2_7_URDU_CLASSIC/build.gradle.kts
          test -f extracted/Transactor_Android_V2_7_URDU_CLASSIC/app/build.gradle.kts

          echo "Transactor V2.7 project verified"

      - name: Build Transactor V2.7 APK
        working-directory: extracted/Transactor_Android_V2_7_URDU_CLASSIC
        run: |
          gradle clean assembleDebug --stacktrace

      - name: Verify APK
        run: |
          APK="extracted/Transactor_Android_V2_7_URDU_CLASSIC/app/build/outputs/apk/debug/app-debug.apk"

          if [ ! -f "$APK" ]; then
            echo "ERROR: APK was not generated"
            exit 1
          fi

          echo "Transactor V2.7 APK successfully generated"
          ls -lh "$APK"

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: Transactor-V2-7-Urdu-Classic-APK
          path: extracted/Transactor_Android_V2_7_URDU_CLASSIC/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
          retention-days: 30
